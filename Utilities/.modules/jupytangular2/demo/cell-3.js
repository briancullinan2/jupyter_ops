/*
var importer = require('./Core')));

$$.async();

// don't do this
Promise.all([
    importer.import(n)
    importer.import(n)
    ]);

// do this instead!
runAllPromises([
    importer.import(n)
    importer.import(n)
    ])
*/

