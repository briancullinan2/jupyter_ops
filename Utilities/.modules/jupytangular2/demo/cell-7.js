/*
var importer = require('./Core')));

$$.async();

// don't do this
Promise.all([
    importer.importNotebook(n)
    importer.importNotebook(n)
    ]);

// do this instead!
runAllPromises([
    importer.importNotebook(n)
    importer.importNotebook(n)
    ])
*/

