// initialize

/*
var execSync = require('child_process').execSync;
var deps = ['npm install', 'JSONStream', 'nel', 'path', 'fs', 'vm', 'glob'];
try {
    require.resolve('JSONStream');
    require.resolve('nel');
} catch (e) {
    execSync(deps.slice(0, 3).join(' '), {stdio: 'inherit'});
}
*/

// $$.done() for our dumb parser
